# This application does not use environment variables for API keys
# API keys are provided by users through the secure UI interface
# and stored only in browser localStorage

# No API keys needed in environment variables!
# Users will input their own keys:
# - Firecrawl API key from https://firecrawl.dev
# - Anthropic API key from https://console.anthropic.com